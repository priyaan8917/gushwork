Project Overview — what the site is and its tech stack
File Structure — table mapping each file to its role
Page Sections — description of all 13 sections (Hero, Specs, Process Tabs, FAQ, Footer, etc.)
Carousel & Zoom — Technical Details — explains the lens, 2.5x preview panel, thumbnail pill, and auto-play logic
JavaScript Function Reference — table of every key function and what it does
CSS Architecture — how styles.css is organized
Customization Guide — how to swap images, change zoom level, update brand colors, and add process tabs
Dependencies & Compatibility — no build tools needed, browser support, how to run locally
Known Limitations — broken local image paths, placeholder form/downloads, zoom clipping edge case
